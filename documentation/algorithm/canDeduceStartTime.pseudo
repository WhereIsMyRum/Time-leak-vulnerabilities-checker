function canDeduceStartTime()
    canDeduce = true

    for place in outputPlaces do
        if (place is not startTimeDeducible)
            canDeduce = false
            break
    end

    return canDeduce